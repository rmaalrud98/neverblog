# 매일 자동 실행 워크플로 (Claude가 이 저장소에서 수행하는 절차)

이 문서는 매일 트리거로 실행되는 Claude 세션이 그대로 따라야 하는 절차입니다.

## 0. 사전 점검
```
node scripts/check-session.js
```
- 종료 코드가 0이 아니면(세션 없음/만료) **글을 쓰지 말고** 사용자에게 알린다:
  "네이버 로그인 세션이 만료됐습니다. 로컬 PC에서 `npm run login-setup`을 다시 실행해
  NAVER_STORAGE_STATE_B64 환경변수를 갱신해주세요."
- 네트워크 정책이 blog.naver.com / nid.naver.com 을 막고 있으면 (연결 자체가 실패하면)
  마찬가지로 글을 쓰지 말고 사용자에게 네트워크 정책 변경을 요청한다.

## 1. 오늘의 경제 이슈 리서치 (WebSearch)
- 목표: **네이버 홈(메인) 경제판 노출**을 노리는 시의성 있는 글.
- 오늘 날짜 기준 한국/글로벌 경제 뉴스를 3~5개 주제로 조사한다.
  (예: 기준금리/통화정책, 환율, 국내외 증시 이슈, 부동산 정책/통계, 고용/물가지표,
  생활경제 이슈 - 세금/연금/대출 등)
- 각 주제마다 핵심 사실(수치, 날짜, 기관명)과 출처를 메모해둔다. **추측으로 숫자를
  지어내지 않는다.** 검색으로 확인 못한 구체적 수치는 글에 넣지 않거나 "[확인 필요]"로
  표시해 사용자가 검수하기 쉽게 한다. 출처 불분명한 "카더라"식 루머는 절대 사실처럼
  쓰지 않는다 (홈판 저품질 판정 1순위).
- 도표/그래프로 그릴 수 있는 **실제 수치 1~3개** (추이나 비교 데이터)를 따로
  정리해둔다 — 4단계에서 `media` 배열을 채우는 재료가 된다. 가능하면 정부/기관
  공식 발표 페이지 URL도 하나 확보해둔다 (있으면 출처 캡처 이미지로 쓸 수 있음).

**키워드 & 홈판 전략** (수강 가이드 반영):
- 주제마다 메인 키워드 1개 + 서브 키워드 여러 개를 정하고, 본문 안에 각 키워드가
  (띄어쓰기/조사 변형 포함) 최소 5번 이상 자연스럽게 등장하도록 쓴다.
- 5개 글 비율은 **롱테일(꾸준히 검색되는 키워드) 3개 : 숏테일(오늘의 이슈성 키워드) 2개**
  정도로 섞는다.
- 5개 중 1개 정도는 경제와 "마음/심리"를 엮은 글로 섞으면 지식판 노출에도 유리하다
  (예: "가난한 사람들의 특징 TOP3", "돈 잘 모으는 사람들의 공통점" 류).
- 경제 주제는 세대(20대 vs 40대 등) 간 입장이 갈릴 수 있는 소재를 적극 활용한다
  (예: "국민연금 개편, 20대 40대 누가 더 이득일까?") — 댓글 유도 = 체류시간 증가.

## 2. 글 5개 작성 — "밍니" 스타일 가이드

실제 블로그(blog.naver.com/mingny_arichiving)에 이미 올라온 글들을 분석해서 정리한
문체 가이드입니다. 이 스타일을 최대한 그대로 따라 씁니다.

**페르소나**: 화자 이름은 "밍니". 20대 후반 여성, 최근 결혼(신혼), 재테크/경제 공부를
막 시작한 초보 컨셉. 친구·지인 얘기를 자주 인용하고, 자기 계좌/상황을 예시로 든다.
(사용자 승인: 캐릭터로 1인칭 경험담을 지어내도 됨.)

⚠️ **단, 즉시적/구체적으로 검증 가능한 행동은 지어내지 않는다.** 네이버 AI가 계정의
실제 결제·예약·위치 등 행동 데이터와 대조해서 "오늘/방금 ~했다"류의 허구 경험을
비정상 데이터로 판정할 수 있다는 게 확인된 리스크다. 그래서:
- "며칠 전에", "얼마 전에", "요즘" 처럼 두루뭉술한 시점 표현만 쓰고, "어제 거기
  가서 결제했는데", "방금 다녀왔는데" 처럼 오늘/어제 시점의 구체적 행동(결제,
  방문, 예약 등)은 지어내지 않는다.
- 대신 생각·의견·고민·문자 받은 것·뉴스 본 것 정도는 자유롭게 지어내도 된다
  (예: "문자가 왔는데", "뉴스 보다가", "저는 ~할 것 같아요").

**도입부**: 최근 있었던 일상 경험담(며칠 전 친구랑 얘기하다가, 얼마 전 문자가 왔는데,
요즘 주변에서 등)으로 시작해서 자연스럽게 오늘 주제로 연결한다. 이 주제를 다루는
개인적인 이유를 먼저 밝힌 뒤 "정리해 봤어요" 식으로 글의 목적을 제시하고 도입부를
":)" 같은 이모티콘으로 마무리.

**제목 짓기** (홈판 노출 핵심):
- 제목에 정답/핵심 수치를 넣지 않는다 — 클릭 전에 궁금증이 남도록 정답 직전에서
  끊는다. (안 되는 예: "국민연금 개편 40대는 이제 끝났습니다" → 되는 예: "국민연금
  개편, 알고 보면 40대가 제일 손해인 이유")
- 세대/입장이 갈릴 수 있는 요소를 넣어 댓글(찬반) 유도. 억지 논쟁거리를 만들지는
  않는다 — 진짜 입장이 갈릴 만한 주제에서만 쓴다.
- 자문: 나라면 클릭할까? 관심 없는 주제여도 클릭할까? 초등학생도 무슨 내용인지
  이해할까?
- 5개 중 3개는 "메인 키워드 + 의문형" 제목, 2개는 순수 의문형 제목으로 섞는다.

**어투**: 부드러운 구어체 존댓말 — "~인데요", "~더라고요", "~인 것 같아요", "~죠".
말줄임표(..)와 이모티콘(ㅎㅎ, ㅠㅠ, :) )을 자연스럽게 섞되 과하게 남발하지 않는다
(문단 3~4개당 1개 정도).

**문단**: 1~2문장마다 줄바꿈. 5줄 넘게 이어지는 문단은 쓰지 않는다. 스크롤하면서
가볍게 읽히는 리듬을 유지.

**소제목**: ■ 같은 기호는 쓰지 않는다. 궁금증을 유발하는 질문형/도발형 문구로 쓴다
(예: "OO, 왜 이렇게 된 거야?", "OO, 진짜 필요할까?"). 글당 2~4개.

**정보 정리**: 항목 나열은 ①②③ 번호나 • 불릿을 활용. 비교가 필요하면 표(마크다운
표 형태) 활용 — 예: "A안 vs B안" 장단점 비교.

**출처**: 정부 발표/기사뿐 아니라 유튜브 경제 채널 인용도 자연스럽다
(예: "출처: 유튜브 <채널명>" 형식으로 문단 사이에 언급). 숫자는 검색으로 확인된
것만 쓰고, 불확실하면 "[확인 필요]"로 표시.

**분량**: 순수 텍스트 기준 800~1,500자 정도. 기존보다 짧게, 대신 문단을 잘게
쪼개서 체감 분량은 유지한다.

**해시태그**: 넣지 않는다. (실제 블로그도 별도 해시태그 목록을 안 씀. 대신
본문 안에서 어울리면 문장 끝에 해시태그 하나 정도 슬쩍 붙이는 건 괜찮음 —
예: "#모두의회고" 처럼.)

**마무리**: 항상 질문을 던지고 "댓글로 남겨 주세요 :)" 류의 문구로 끝낸다.
저자 개인의 선택/의견을 한 줄 덧붙인다 (예: "저는 ~할 것 같아요").

**카테고리 분산**: 같은 날 5개 글의 주제는 서로 겹치지 않게 분산 (금리/부동산/
증시/환율/생활경제 등). 각 글에 카테고리 하나를 붙인다 (예: 트렌드/이슈, 주식,
경제심리, 지원금, 생활, 정책, 금융 등 — 실제 블로그에서 쓰던 카테고리명 참고).

## 3. 절대 금지 사항 (수강 가이드 반영)

아래에 해당하면 홈판 저품질 판정을 받거나(사용자 요청 취지에도 어긋나므로) 절대
쓰지 않는다:

- 특정 지역·세대·성별을 겨냥한 혐오·비하 표현, 근거 없는 광역 비난
  (예: "OO 세대는 다 ~하다" 식 단정)
- 정치 관련 글 (특히 중국 비판 등 민감한 소재)
- 출처 불분명한 루머를 사실처럼 전달 ("정부가 곧 ~한대요" 식 카더라)
- 욕설·비속어, 조롱성 캡처/비하 글
- 자살·자해 등 생명 관련 암시나 조롱
- 특정 인물 외모·신체를 선정적으로 언급하는 글
- 세대 간 입장 차이를 다루더라도 혐오로 넘어가지 않는다 (댓글 유도 ≠ 갈등 조장)

## 4. 스키마에 맞춰 저장
`posts/sample.json` 의 스키마를 그대로 따라 `posts/<오늘날짜 YYYY-MM-DD>.json` 로 저장한다.
(해시태그를 안 쓰기로 했으므로 `tags`는 빈 배열로 둔다. `heading: true` 문단은
자동화가 "■" 없이 그대로 타이핑한다 — 소제목 자체를 질문형/도발형 문구로 쓴다.
`category`는 썸네일 이미지에 라벨로도 쓰인다.)

리서치 단계(1단계)에서 확보한 **실제 수치**를 `media` 배열에 채워 넣는다 — 이게
있어야 5단계에서 진짜 도표/그래프가 나온다 (없으면 최소한의 숫자 카드로만 채워짐):
- `{ "type": "line", "title": "...", "labels": [...], "values": [...], "unit": "%" }` — 추이
- `{ "type": "bar", "title": "...", "labels": [...], "values": [...], "unit": "%" }` — 비교
- `{ "type": "stat", "value": "2.75%", "caption": "..." }` — 핵심 수치 하나만 강조
- `{ "type": "screenshot", "url": "https://...", "selector": "선택자(선택)" }` — 공식
  출처 페이지 실제 캡처 (정부/기관 발표 페이지처럼 출처가 분명한 곳만. 뉴스 기사
  사진·인물 사진 등 저작권 있는 이미지는 캡처하지 않는다)
- `media` 항목 개수는 소제목 개수에 맞춘다 (소제목마다 이미지 1개씩 들어감).

```json
{
  "posts": [
    { "title": "...", "category": "경제", "tags": [],
      "media": [
        { "type": "line", "title": "...", "labels": ["..."], "values": [0], "unit": "%" }
      ],
      "paragraphs": [
        { "text": "질문형 소제목", "heading": true },
        "본문 문단 (1~2문장, 짧게)...",
        "다음 문단..."
      ]}
  ]
}
```

## 5. 이미지 자동 생성
```
node scripts/generate-images.js posts/<오늘날짜>.json
```
- **썸네일(대표 이미지) 1장만** 큰 텍스트가 들어간다 — 배경은 밋밋한 단색이 아니라
  스카이라인/그래프 라인/동전 같은 경제 테마 일러스트를 직접 그려서 깔아준다.
  (뉴스/타사 이미지를 긁어오지 않아 저작권·유사문서 문제가 없음.)
- **본문 이미지(소제목마다 1장)는 텍스트 카드가 아니라 실제 도표·그래프**다.
  `media` 배열의 수치로 막대/꺾은선 그래프를 그리거나, 지정한 공식 출처 URL을
  실제로 캡처한다. `media`가 비어 있으면 최소한의 숫자 카드로 대체된다 —
  가능하면 항상 `media`를 채워서 진짜 그래프가 나오게 한다.
- 미드저니·ImageFX 같은 AI 이미지 생성 서비스는 스크립트로 호출 가능한 공개
  API가 없어서 자동화에 쓸 수 없다. 대신 실제 데이터 기반 도표/그래프와 공식
  출처 캡처로 대체한다.
- 최소 5장(썸네일 포함)을 `posts/images/<날짜>/post-<n>/` 에 저장하고, 각 post
  객체에 `images` 배열(파일 경로)을 채워 파일에 다시 저장한다.
- 출처 페이지 캡처(`screenshot` 타입)는 네트워크/페이지 구조 문제로 실패할 수
  있다 — 실패하면 자동으로 숫자 카드로 대체되니 신경 쓰지 않아도 된다.

## 6. 네이버 블로그에 임시저장으로 업로드
```
node scripts/post-drafts.js posts/<오늘날짜>.json
```
- 이 스크립트는 **"저장" 버튼만** 누르고 "발행" 버튼은 절대 누르지 않는다 (코드에 하드코딩된 안전장치).
- 5단계에서 만든 `images` 배열을 보고, 대표 이미지 1장은 본문 맨 앞에, 나머지는
  각 소제목 앞에 자동으로 삽입한다 (네이버 에디터의 "사진" 버튼을 자동 클릭).
- 실패한 글이 있으면 `posts/debug/` 의 스크린샷을 참고해 원인을 파악하고, 선택자(selector)가
  네이버 UI 변경으로 어긋난 것 같으면 `scripts/post-drafts.js` 를 그에 맞게 고친다.
  안 되면 무리해서 재시도하지 말고 실패로 보고한다. 이미지 삽입만 실패해도 텍스트
  저장 자체는 계속 진행되니, 이미지 삽입 실패는 경고로만 남기고 전체 실패로
  치지 않는다.
- 임시저장은 순서대로 바로바로 진행하되, **실제 발행은 사용자가 나중에 수동으로** 한다.
  발행 시 한 번에 몰아서 올리기보다 시간 간격(예: 1시간 간격)을 두고 발행하는 게
  좋다는 점을 결과 보고에 참고로 덧붙인다.

## 7. 결과 보고
- 성공/실패 개수, 실패 사유, 임시저장함 링크(`https://blog.naver.com/{blogId}?Redirect=Dlog`)를
  사용자에게 간단히 요약해 알린다.
- 전부 성공했으면 "오늘 글 5개 임시저장 완료, 확인 후 발행하세요" 정도로 짧게.
