# 매일 자동 실행 워크플로 (Claude가 이 저장소에서 수행하는 절차)

이 문서는 매일 트리거로 실행되는 Claude 세션이 그대로 따라야 하는 절차입니다.

## 0. 사전 점검
```
node scripts/check-session.js
```
- 종료 코드가 0이 아니면(세션 없음/만료) **글을 쓰지 말고** 사용자에게 알린다:
  "네이버 로그인 세션이 만료됐습니다. 로컬 PC에서 `npm run login-setup`을 다시 실행해
  NAVER_STORAGE_STATE_B64 환경변수를 갱신해주세요."
- 네트워크 정책이 blog.naver.com / nid.naver.com 을 막고 있으면 (연결 자체가 실패하면)
  마찬가지로 글을 쓰지 말고 사용자에게 네트워크 정책 변경을 요청한다.

## 1. 오늘의 경제 이슈 리서치 (WebSearch)
- 목표: **네이버 홈(메인) 경제판 노출**을 노리는 시의성 있는 글.
- 오늘 날짜 기준 한국/글로벌 경제 뉴스를 3~5개 주제로 조사한다.
  (예: 기준금리/통화정책, 환율, 국내외 증시 이슈, 부동산 정책/통계, 고용/물가지표,
  생활경제 이슈 - 세금/연금/대출 등)
- 각 주제마다 핵심 사실(수치, 날짜, 기관명)과 출처를 메모해둔다. **추측으로 숫자를
  지어내지 않는다.** 검색으로 확인 못한 구체적 수치는 글에 넣지 않거나 "[확인 필요]"로
  표시해 사용자가 검수하기 쉽게 한다.

## 2. 글 5개 작성
각 글은:
- 분량: 1,500~2,500자
- 구조: 후킹 도입부 → 소제목(■) 3~4개 → 각 소제목 아래 설명 문단 → 마무리 요약
- 톤: 경제 뉴스를 "설명/해석"하는 블로그 글 (단순 기사 복붙 금지, 자기 언어로 재구성)
- 해시태그 8~10개 (예: #경제, #재테크, #기준금리, #부동산, #주식 + 주제별 키워드)
- 같은 날 5개 글의 주제는 서로 겹치지 않게 분산 (금리/부동산/증시/환율/생활경제 등)

## 3. 스키마에 맞춰 저장
`posts/sample.json` 의 스키마를 그대로 따라 `posts/<오늘날짜 YYYY-MM-DD>.json` 로 저장한다.

```json
{
  "posts": [
    { "title": "...", "tags": ["..."], "paragraphs": [
      { "text": "소제목", "heading": true },
      "본문 문단..."
    ]}
  ]
}
```

## 4. 네이버 블로그에 임시저장으로 업로드
```
node scripts/post-drafts.js posts/<오늘날짜>.json
```
- 이 스크립트는 **"저장" 버튼만** 누르고 "발행" 버튼은 절대 누르지 않는다 (코드에 하드코딩된 안전장치).
- 실패한 글이 있으면 `posts/debug/` 의 스크린샷을 참고해 원인을 파악하고, 선택자(selector)가
  네이버 UI 변경으로 어긋난 것 같으면 `scripts/post-drafts.js` 를 그에 맞게 고친다.
  안 되면 무리해서 재시도하지 말고 실패로 보고한다.

## 5. 결과 보고
- 성공/실패 개수, 실패 사유, 임시저장함 링크(`https://blog.naver.com/{blogId}?Redirect=Dlog`)를
  사용자에게 간단히 요약해 알린다.
- 전부 성공했으면 "오늘 글 5개 임시저장 완료, 확인 후 발행하세요" 정도로 짧게.
